import { SolutionDetailMain } from "@/components/giai-phap/solution-detail-main";

export function SolutionDetailPage({ articleId }: { articleId: string }) {
  return <SolutionDetailMain articleId={articleId} />;
} 